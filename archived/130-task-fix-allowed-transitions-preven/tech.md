Bug: In tasks_ai/constants.py, ALLOWED_TRANSITIONS['TESTING'] includes 'STAGING', allowing direct transition without passing through REVIEW. This bypasses the regression check gate enforced in REVIEW state (see cli.py lines 1850-1861). Correct workflow: TESTING -> REVIEW (run regression diff, set Rc flag) -> STAGING. Fix: Remove 'STAGING' from the allowed list for TESTING in constants.py. Validation: run 'hammer check lint', 'hammer check test', update tests in tests/test_all_transitions.py if present.