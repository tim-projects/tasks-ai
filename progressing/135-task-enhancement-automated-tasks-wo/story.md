We have a method to save the current .tasks worktree to a git branch called tasks on the remote. This needs expansion: maintain both local and remote 'tasks' branches. Whenever a task is archived, automatically push the current .tasks worktree to these branches via the existing save command. Also add a restore command to manually pull from these branches to recover a deleted .tasks folder.