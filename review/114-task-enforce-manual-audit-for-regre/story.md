The current implementation of the 'regression-check' gate allows automated agents to perform an audit and mark it as passed in a single, unverified step. This bypasses the intended 'human-in-the-loop' review process required for critical pipeline gates. We need a verifiable audit trail that requires an explicit, authenticated acknowledgment ('repo audit --ack') of the generated diff patch before the state-machine gate can be unlocked.