diff --git a/repo.py b/repo.py
index dfeac9a..249db03 100644
--- a/repo.py
+++ b/repo.py
@@ -260,11 +260,12 @@ def cmd_merge(src_input, target_input, auto_commit=True):
         context=f"merge {src}→{target}",
     )
     if check_remote_exists():
-        # Check if push is actually necessary
-        result = run(["git", "log", f"{PRIMARY_REMOTE}/{target}..{target}", "--oneline"], capture=True)
-        if not result.stdout.strip():
-            info(f"Branch '{target}' is up-to-date with remote. Skipping push.")
-        elif FLAGS["yes"] or prompt_yes_no(f"Push {target}?"):
+        st = run(["git", "status", "--porcelain"], capture=True).stdout.strip()
+        if target == "main" or st:
+            if FLAGS["yes"] or prompt_yes_no(f"Push {target}?"):
+                run(["git", "push", PRIMARY_REMOTE, target], context=f"push {target}")
+        else:
+            log(f"No local changes found on {target}. Auto-pushing...")
             run(["git", "push", PRIMARY_REMOTE, target], context=f"push {target}")
     else:
         warn("No remote - skipping push")
diff --git a/tasks_ai/cli.py b/tasks_ai/cli.py
index 6680f67..15b735a 100644
--- a/tasks_ai/cli.py
+++ b/tasks_ai/cli.py
@@ -271,8 +271,7 @@ class TasksCLI:
         debug_log2 = f"/tmp/review_diff_debug_{os.getuid()}.log"
         try:
             with open(debug_log2, "a") as f:
-                f.write(
-                )
+                f.write(f"branch={branch}, default_branch={default_branch}, main_sha={main_sha}\n")
         except (PermissionError, OSError):
             pass
 
@@ -433,22 +432,21 @@ class TasksCLI:
             print(message)
 
     def error(self, message, hint=None):
-        if hint:
-            message = f"{message} | HINT: {hint}"
         if self.quiet:
             pass
         elif self.as_json:
-            print(
-                json.dumps(
-                    {
-                        "success": False,
-                        "error": message,
-                        "messages": self.output_messages,
-                    }
-                )
-            )
+            response = {
+                "success": False,
+                "error": message,
+                "messages": self.output_messages,
+            }
+            if hint:
+                response["hint"] = hint
+            print(json.dumps(response))
             sys.exit(1)
         else:
+            if hint:
+                message = f"{message} | HINT: {hint}"
             print(f"Error: {message}", file=sys.stderr)
             sys.exit(1)
 
@@ -2344,8 +2342,10 @@ class TasksCLI:
             pass
         else:
             term_width = shutil.get_terminal_size(fallback=(180, 24)).columns
-            fixed_cols = 3 + 1 + 2 + 1 + 6 + 1 + 25  # id(3) + space + priority(2) + space + type(6) + space + space(for branch padding)
-            branch_min = 25
+            fixed_cols = (
+                3 + 1 + 2 + 1 + 6 + 1 + 25
+            )  # id(3) + space + priority(2) + space + type(6) + space + space(for branch padding)
+
             summary_min = 30
             # Available for summary + branch
             # Available for summary + branch
@@ -2355,11 +2355,11 @@ class TasksCLI:
             summary_width = max(summary_min, available - branch_width)
 
             # Color constants with backgrounds
-            C_HEADER = "\033[1;47;30m" # Bold Black on White
+            C_HEADER = "\033[1;47;30m"  # Bold Black on White
             C_STATE = "\033[1;44;37m"  # Bold White on Blue
-            C_ID = "\033[1;32m"      # Bright Green
-            C_PRIO = "\033[1;35m"    # Bright Magenta
-            C_TYPE = "\033[36m"      # Cyan
+            C_ID = "\033[1;32m"  # Bright Green
+            C_PRIO = "\033[1;35m"  # Bright Magenta
+            C_TYPE = "\033[36m"  # Cyan
             C_RESET = "\033[0m"
 
             for state, tasks in all_data.items():
diff --git a/check_cols.py b/check_cols.py
index 07cd98e..b74633a 100644
--- a/check_cols.py
+++ b/check_cols.py
@@ -1,9 +1,12 @@
 import os
-import sys
+
+
 # Simulate what hammer does
 def get_terminal_width():
     try:
         return os.get_terminal_size().columns
     except OSError:
         return 80
+
+
 print(get_terminal_width())
diff --git a/check_env.py b/check_env.py
index e4b4bb4..11fcca2 100644
--- a/check_env.py
+++ b/check_env.py
@@ -1,2 +1,3 @@
 import os
+
 print(f"COLUMNS: {os.environ.get('COLUMNS')}")
diff --git a/check_shutil.py b/check_shutil.py
index f7ff4e9..46891e3 100644
--- a/check_shutil.py
+++ b/check_shutil.py
@@ -1,4 +1,5 @@
 import shutil
 import os
+
 print(f"os.get_terminal_size: {os.get_terminal_size().columns}")
 print(f"shutil.get_terminal_size: {shutil.get_terminal_size().columns}")
diff --git a/check_width.py b/check_width.py
index 9671810..949c231 100644
--- a/check_width.py
+++ b/check_width.py
@@ -1,7 +1,11 @@
 import os
+
+
 def get_terminal_width():
     try:
         return os.get_terminal_size().columns
     except OSError:
         return 80
+
+
 print(f"term_width = {get_terminal_width()}")
