The _sync_task_content method in tasks_ai/cli.py currently only syncs git commits and notes from current-task.md. It does NOT check for unstaged file changes in the task directory. Enhancement: Use git status or git diff to detect unstaged changes on .md files within the task directory. If any are found, reload those files from disk and update task.parts before returning True so checkpoint commits them. Path: tasks_ai/cli.py::_sync_task_content.