- [ ] 1. All tests in tests/ pass without modification to  or  to bypass validation. 2. Detected tools:
  type_check: pyright

Would you like to save this configuration?
Run: tasks config set repo.lint <tool>
      tasks config set repo.type_check pyright
      tasks config set repo.test <tool>
      tasks config set repo.format <tool> automatically configures valid tool paths in the test repository.