The pipeline currently lacks strict validation on state transitions and archive eligibility. Task 119 was archived while still on a feature branch (not merged to main), and state moves (PROGRESSING -> REVIEW) bypassed mandatory validation gates like TESTING. This research task aims to audit all state machine transitions in 'tasks.py' and 'repo.py', identify vulnerable bypasses, and implement comprehensive test coverage for all valid and invalid state transitions.