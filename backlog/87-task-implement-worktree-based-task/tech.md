Add worktree creation/deletion commands to tasks.py, modify repo.py to detect and operate within worktrees, update file paths to be worktree-relative where appropriate, track worktree paths in task metadata, enhance repo.py merge logic to handle worktree-to-main merges, implement automated conflict detection and reporting, provide tools for manual conflict resolution, implement automated worktree removal after successful merge.